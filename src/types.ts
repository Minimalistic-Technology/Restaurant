export interface MenuItem {
    title: string;
    desc: string;
    price: string;
    img: string;
    rating: number;
}

export interface galleryItems {
    "id": number;
    "title": string;
    "img": string;
}